export const { PORT = 3000 } = process.env;
export const { ADDRESS = 'mongodb://localhost:27017' } = process.env;
export const { DATA_BASE = 'mestodb' } = process.env;
export const { SECRET_KEY = 'super-strong-secret' } = process.env;
